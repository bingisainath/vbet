import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        padding: 15
    },
    subContainer: {
        flexDirection: 'row'
    },
    textStyle: {
        color: '#000'
    },
    errorText: {
        color: 'red',
        fontWeight: '400',
        fontSize: 12,
    },
})