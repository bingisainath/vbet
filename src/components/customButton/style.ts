import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    mainContainer: {
        alignItems: 'center',
    },
    container: {
        height: 40,
        paddingVertical: 6,
        paddingHorizontal: 12,
        alignItems: 'center',
        borderRadius: 10,
        borderWidth: 1,
    },
    text: {
        fontWeight: 'bold',
        color: 'black',
        fontSize: 16,
    },
})